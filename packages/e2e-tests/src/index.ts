describe('Sample Application E2E', () => {
  it('should run sample and print hello', () => {
    runSample();
  });
});
// e2e test entry
import { runSample } from 'sample-application';

describe('Sample Application E2E', () => {
  it('should run sample and print hello', () => {
    runSample();
  });
});
