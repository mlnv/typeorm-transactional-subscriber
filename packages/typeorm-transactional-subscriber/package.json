{
  "name": "typeorm-transactional-subscriber",
  "description": "Adds afterInsertCommitted, afterUpdateCommitted, and afterRemoveCommitted hooks to TypeORM subscribers, ensuring side effects only run after a successful transaction commit (never on rollback or savepoint release).",
  "version": "0.1.3",
  "repository": {
    "type": "git",
    "url": "https://github.com/mlnv/typeorm-transactional-subscriber.git"
  },
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsc -b",
    "clean": "rimraf dist",
    "lint": "pnpm exec eslint .",
    "lint:fix": "pnpm exec eslint --fix ."
  },
  "license": "MIT",
  "author": "Maksym Yemelianov",
  "dependencies": {
    "typeorm": "^0.3.26"
  },
  "devDependencies": {
    "@types/node": "^24.3.0"
  }
}
